# Deploy Notes - 2026-06-15 10:01

## Change
禁用前端兜底数据逻辑。

## Scope
- `lib/storage.ts`
  - PocketBase 不可用时不再读取/写入 localStorage 业务状态。
  - `getInitialState()` 改为空运行态，不再返回 `SEED_PLAYERS` 或内置 `QUESTIONS`。
  - 注册、提交结果、后台开关等写操作在 PocketBase 不可用时抛出“后端连接不可用，请稍后重试”。
  - 查询类接口在 PocketBase 不可用时返回空态/空列表，不再使用种子数据或本地业务状态。
- `lib/pb-storage.ts`
  - PocketBase 查询当前用户失败或不可用时，不再用本地缓存玩家兜底。
  - 注册在 PocketBase 不可用时直接报错，不再创建本地玩家。
  - 大厅快照不再用本地缓存玩家兜底。
- `tsconfig.json`
  - 之前已排除 PocketBase 备份迁移目录，避免前端构建扫描后端备份文件。

## Verification
- `npm run build` 成功。
- PM2 `qhduan-frontend` online。
- `http://127.0.0.1:3000/` 返回 200。
- `http://127.0.0.1/` 返回 200。
- 已清理 Nginx qhduan 页面/API 缓存并 reload，80 与 3000 ETag 一致。

## Safety
- 未修改 `pb_data`。
- 未停止 PocketBase。
